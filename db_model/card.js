'use strict';

var db = require('./db');

var user = {
    createCard: function (cardDetails, cb) {

    },
    validateCard: function (cardDetails, cb) {

    },
    disableCard: function (cardDetails, cb) {

    },
    deleteCard: function (cardDetails, cb) {

    },
    isImageURLValid: function (cardDetails, cb) {

    }
};

module.exports = card;